<div id="poll-container">
    <h1 class="text-4xl font-bold">
        Paredão das Linguagens: Vote para eliminar.
        <%= @poll_options.pluck(:name).to_sentence(last_word_connector: ' ou ') + '?' %>
    </h1>
    <div class="flex flex-col gap-4 w-100 mt-8">
        <% @poll_options.each do |item| %>
            <%= button_to(vote_url,
                          method: :post,
                          params: { poll_option_id: item.id },
                          class: 'flex-grow flex items-center border border-gray-600 text-left p-4',
                          form_class: 'w-100 flex') do
            %>
                <h2 class="text-2xl text-slate-800 font-bold flex-grow">
                    <%= item.name %>
                </h2>
                <%= image_tag(item.photo, class: 'w-16 h-16 object-contain') %>
            <% end %>
        <% end %>
        <%= button_to(ranking_url,
                        method: :post,
                        class: 'block text-center p-4 text-white bg-violet-800 hover:bg-violet-900 flex-grow',
                        form_class: 'w-100 flex flex-grow') do
        %>
            Ver ranking
        <% end %>

    </div>
</div> 